<div class="row">
  <div class="col-md-12">
    <h2> <%= @post.title %> </h2>
    <h5> By <%= @post.author %> on <%= post.updated_at.strftime('%m/%d/%Y') %> </h5>
    <div class="content">
      <%= @post.content %>
    </div>
  </div>
</div>
